package service;

import dto.TokenDTO;

import java.io.IOException;
import java.util.ArrayList;

public class SyntacticAnalysisService {

    /*
    public void syntaticAnalysis(ArrayList<TokenDTO> tokens){
        ArrayList<String> stack = new ArrayList<>();

        // ler o primeiro simbolo
        stack.add(tokens.getFirst().getToken());
        while (stack.size() != 2 && stack.get(stack.size() - 1) != "E" && !tokens.isEmpty()){
            String stackTop = String.valueOf(stack.size() - 1);

            // verificar se a ação é empilhar
            if () {

            // verificar se a ação é reduzir
            } else if () {

            // verificar se o codigo foi inteiramente lido
            } else if () {

            } else {
                throw new IllegalArgumentException("Syntatic Analisys Error");
            }
        }
    }
    */

    public static void main(String[] args) throws IOException {

    }
}
